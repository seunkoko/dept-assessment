// eslint-disable-next-line
import React from 'react';

import './Button.scss';

const ButtonViewCase = (props) => {
	return (
    <button
      type="button"
      className="btn button-style view-case-style"
    >
      VIEW CASE
    </button>
	);
};

export default ButtonViewCase;
